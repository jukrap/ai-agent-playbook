# Existing repositories

Bootstrap preserves existing root instructions and playbook records. It creates only CURRENT.md and metadata for a project without records. Layout migration preserves user content and requires owned unchanged metadata; record reads work without migration. Use lifecycle.md for preview and recovery.
