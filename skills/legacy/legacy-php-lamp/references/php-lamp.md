# PHP LAMP Stack Checks

- Trace entry scripts, include order, shared config, session start, auth checks, globals, and form posts together.
- Check direct SQL, escaping, character encoding, timezone, path assumptions, file uploads, and output buffering.
- Preserve parameter names, session keys, include order, and deployed PHP version constraints.
- Verify GET, POST, validation failure, auth/session, database side effects, and hosting-specific behavior when relevant.

