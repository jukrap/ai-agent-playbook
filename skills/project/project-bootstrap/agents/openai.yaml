openai:
  short_description: "Set up project agent memory and root instructions"
  default_prompt: "Use project-bootstrap to inspect this repository and propose only the .ai-agent-playbook and root agent docs it actually needs."
