# Local AI Compatibility Note

`templates/local-ai` is kept only as a compatibility pointer for older projects.

New projects should use `templates/project-playbook/` and copy it into the target repository as `ai-playbook/`.

Former long-form helper docs now live under `templates/project-playbook/guides/`.
