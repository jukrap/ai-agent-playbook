# Documentation Package

Inputs: changed behavior, user-facing scope, setup commands, known constraints.

Outputs: README or docs update, map update, handoff note when needed.

Skills: project doc system, repo onboarding, documentation review.

Tools: `catalog list`, `operator search`, `operator map`, link checks when available.

Stop conditions: docs contradict code, unknown setup path, private information risk.

Verification: follow documented commands or validate examples against current files.

