# Dotnet WebForms Stack Checks

- `.aspx`, code-behind, designer file, master page, user control, event handler, validation control을 함께 추적합니다.
- ViewState, postback order, data binding, Web.config, handler/module, IIS setting, deployment transform을 확인합니다.
- control ID, server-side event name, session key, framework version constraint를 보존합니다.
- 관련되면 initial load, postback, validation failure, auth/session, deployed-framework compatibility를 검증합니다.

