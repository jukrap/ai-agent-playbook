# jQuery Browser Stack Checks

- active HTML/template, script include order, selector, delegated event, plugin initialization, AJAX call, global을 추적합니다.
- ID, class, data attribute, plugin lifecycle, CSS hook, JavaScript에 embedded된 server-rendered value를 보존합니다.
- duplicate handler, re-initialization bug, old syntax constraint, hidden table/modal/date picker coupling을 주의합니다.
- load, changed interaction, repeated action, console, network, validation failure, dynamic DOM update를 검증합니다.

