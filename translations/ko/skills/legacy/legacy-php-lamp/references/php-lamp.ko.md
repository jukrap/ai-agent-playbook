# PHP LAMP Stack Checks

- entry script, include order, shared config, session start, auth check, global, form post를 함께 추적합니다.
- direct SQL, escaping, character encoding, timezone, path assumption, file upload, output buffering을 확인합니다.
- parameter name, session key, include order, deployed PHP version constraint를 보존합니다.
- 관련되면 GET, POST, validation failure, auth/session, database side effect, hosting-specific behavior를 검증합니다.

