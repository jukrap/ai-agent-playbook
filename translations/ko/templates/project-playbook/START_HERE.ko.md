# 여기서 시작

다음 에이전트가 특정 상태에서 이어서 작업해야 할 때 이 파일을 갱신합니다.

이 파일은 짧게 유지합니다. Worklog, plan, architecture map이 아니라 포인터입니다.

## 현재 목표

- 진행 중인 작업을 한두 개 bullet로 적습니다.

## 먼저 읽을 것

- `CURRENT.md`
- 관련 map 또는 runbook:
- 관련 진행 중 계획:
- 최신 worklog summary:

## 에이전트 진입 체크리스트

- 도구를 고르기 전에 루트 `AGENTS.md`와 더 가까운 하위 에이전트 지침을 확인합니다.
- 계획하거나 수정하기 전에 `CURRENT.md`와 `questions.md`를 읽습니다.
- 코드 작업이라면 관련 `memory/context/`, `maps/`, `contracts/`, workflow recipe를 확인합니다.
- 오래되었거나 위험하거나 낯선 작업이라면 수정 전에 read-only operator 점검을 실행합니다.
- `runtime/` 보고서는 검토된 사실이 `memory/` 또는 `knowledge/`로 승격되기 전까지 생성된 근거로만 다룹니다.

## 다음에 실행할 가능성이 큰 명령

```bash
# 이 프로젝트의 다음 유용한 명령으로 바꿉니다.
```

## 잊지 말 것

- 로컬 전용 정책:
- 검증 명령:
- 알려진 리스크:

## 갱신 규칙

- 긴 이력을 덧붙이지 말고 낡은 bullet을 교체합니다.
- 내용을 중복하지 말고 active plan, map, runbook, 최신 worklog로 링크합니다.
- 진행 중인 작업이 없으면 그 사실을 분명히 쓰고 `CURRENT.md`로 안내합니다.
