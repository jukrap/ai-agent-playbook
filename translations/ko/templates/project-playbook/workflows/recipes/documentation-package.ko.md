# Documentation Package

Inputs: changed behavior, user-facing scope, setup commands, known constraints.

Outputs: README 또는 docs update, map update, 필요 시 handoff note.

Skills: project doc system, repo onboarding, documentation review.

Tools: `catalog list`, `operator search`, `operator map`, 사용 가능한 link check.

Stop conditions: docs와 code의 불일치, 알 수 없는 setup path, private information risk.

Verification: 문서화한 command를 따라가거나 example을 현재 file 기준으로 검증합니다.

