# Repo Onboarding

Inputs: target repository, current task, available docs, known constraints.

Outputs: context note, system map, verification command list, runtime index preview.

Skills: repo onboarding, project bootstrap, project doc system.

Tools: `catalog list`, `layout status`, `index build`, `operator map`, `operator audit`.

Stop conditions: repository 누락, 충돌하는 local instruction, 불명확한 write scope.

Verification: 발견한 project check를 실행하거나 사용할 수 없는 이유를 기록합니다.

